#include <stdio.h>

/**
 * Main - Prints the name of the file program
 * was compiled from, followed by a new line.
 *
 * Return: Always ).
 */
int main(void)
{
	printf("%s\n", __FILE__);

	return (0);
}
