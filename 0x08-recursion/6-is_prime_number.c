#include "main.h"

/**
 * prime_check - checks to see if number is prime
 * Return: 1 if prime, 0 if not
 */
int is_prime_number(int n)
{
	if(num == 1)
		return 0;
	else
		if(num%i == 0)
			return 1;
}

/**
 * s_prime_number - states if number is prime
 * @n: number to check
 * Return: 1 if prime, 0 if not
 */
int is_prime_number(int n)
{
	if (n == 2)
		return (1);
}
